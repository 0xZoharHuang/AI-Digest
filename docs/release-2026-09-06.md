# Local production deployment — 2026-09-06

Deployed code: `4a78bf219082`, immutable app snapshot `app-4a78bf219082-20260905T182711Z`.
The working branch is `codex/phase2-attention-editor`. No cloud deployment was introduced.

## Production contract

- Phase 2 labels and stores all captured normalized inputs. Natural package count is independent of research capacity.
- The classifier remains `gpt-5.6-luna / medium`, text-only, with production concurrency 4.
- Candidate matching uses the pinned local Qwen3 embedding model; ambiguous bare links cannot join otherwise separate objects.
- Phase 3 admission uses bounded `gpt-5.6-sol / high` shortlisting, not an unbounded whole-catalog prompt.
- Research remains `gpt-5.6-sol / medium`, one independent lead per selected package, daily limit 15 and concurrency 3.
- Brief generation remains the existing `gpt-5.6-terra / high` configuration.
- Unselected packages remain stored and unresearched; they are not reclassified as low-value or deleted.

## Verified evidence

| Check | Result |
|---|---|
| Regression suite | 225 passed; 85.85% measured coverage |
| Frozen full corpus | 6,878 units; 6,130 packages |
| Evidence-reviewed sample retention | 193/195, 98.97% |
| Evidence-reviewed same-object recall | 110/113, 97.35% |
| Different-object separation | 118/118, 100% |
| Full-catalog admission | All 6,130 cards considered; 15 selected; 7 bounded calls across 2 levels |
| Installed integration smoke | 3 reports, zero failures, queue traversal and in-memory Wiki verification passed |
| Latest real research | 15 main reports and 3 subreports; zero research failures |
| Live publication | Latest run wrote and read back 21 Wiki pages, then sent the configured bot DM |
| Idempotent publication replay | Zero network calls and no duplicate message |
| Background environment | One-shot launchd probe completed authenticated Luna/medium execution and Lark verification, then sent the actual deployment-complete notice |
| Production services | tick, agent-runner and recover loaded from the deployed snapshot; initial exits 0 |
| Active queues at handoff | staging, jobs, retry_wait, completed and publish_pending empty |

Semantic metrics use model-assisted references with original-evidence adjudication, not human-certified
gold or a guarantee of perfect classification. The original unadjudicated reference and seven uncertain
same-disclosure cases remain preserved; see the Phase 2 validation history for methodology and intervals.

## Budget-preserving backlog completion

| Input day | Normalized units | Stored packages | Research leads |
|---|---:|---:|---:|
| 2026-09-03 | 2,808 | 2,437 | 0 |
| 2026-09-04 | 7,081 | 5,468 | 0 |
| 2026-09-05 | 4,532 | 3,667 | 15 |

All three days were published and archived through the normal recovery path. The two historical days
only received classification and a transparent zero-research status, as agreed. Production configuration
still contains the default 15-package limit; the historical overrides were in-memory and job-scoped.

The latest day's genuinely cold Phase 2 run took 3,470.73 seconds at concurrency 4: 10,681,627 input
tokens (1,957,632 cached) and 371,232 output tokens. Cache-only replay time must not be reported as
cold throughput. No million-record daily throughput guarantee is made.

## Deployment repairs and remaining operating conditions

- A real-data handoff exposed chatter outside the candidate catalog; workspace materialization now
  traverses only eligible catalog entries. Research had not started when this initialization failed.
  Its evidence was retained, a real retry notice was sent, and the successful retry reused Phase 2.
- Downstream failures no longer overwrite a completed Phase 2 marker with legacy fallback output.
- Persisted launchd disabled overrides initially rejected bootstrap. Only the two previously disabled
  AI Digest consumer labels were enabled, after which local cutover succeeded.
- Seven old immutable program snapshots were removed by the configured retention policy. Runtime
  data, research results, evaluation evidence and SQLite backups were preserved.
- The validated `app-f2c555ea20c0-20260905T173316Z` snapshot is the rollback target. The prior rollback
  record was saved in the deployment journal before updating it.
- Source collection is not presented as perfect: the latest day recorded article-body extraction
  failures and two GitHub 404s, preserving metadata and publishing an overall partial-source status.

Private operational receipts, exact Wiki/message IDs, rollback history and service evidence live under
the runtime's `deployments/20260906-phase2/` directory. The temporary background-probe service was
unloaded after its successful check; its scripts and receipts remain for audit.

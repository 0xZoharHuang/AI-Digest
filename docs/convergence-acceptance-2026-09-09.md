# Phase 2/3 收敛优化验收（2026-09-09）

## 范围与发布决定

目标是深度优先、每天最多15个独立研究thread，不设置token、工具调用或compaction次数门槛。
保留当前模型档位、最多6并发、20包上限及既有工作量加权分配。原始记录、历史报告、Wiki结构、
接收者与7点日程不变，不接入新提供商。本轮不再增加第三版算法/提示候选。

**Phase 2阅读视图保持关闭。** 两版候选虽有输入成本改善，但第三天出现具体错误合并；不能满足
“无新增严重错误合并”的门槛。**Phase 3精简研究合同默认关闭，最终成对复核记录单独存档。**
已观察到20包压力测试的来源归属回退，不能用平均分或完整台账抵消。默认保留生产研究合同。

独立保留的工程改进：冻结已调度任务清单；15任务/6并发/20包上限；禁止子agent；原thread恢复；
缺失thread身份时拒绝另建替代thread；已验证产物、共享输入及附件在正常返回和中断时均做哈希检查；
增补内部未成稿判断供brief区分资料不足与编辑取舍；仅记录compaction和用量，不控制Codex压缩。
新阅读视图、精简合同及manifest编译器保持独立默认关闭开关，旧任务按已冻结版本恢复。

## Phase 2全流程成本

三天为9月6/7/8日，3,217/3,098/3,766个标准化单元。9月6日采用此前已验证的named-primary baseline，
不是当天更早的另一种聚类实现。最终候选运行前后实现哈希一致。包括标注、排除复核、对象归并、
别名及歧义复核；缓存记录按原模型调用计算，不等于本次重放账单或订阅额度。

| 模型/档位 | 基线非缓存输入 | 候选非缓存输入 | 基线输出 | 候选输出 | 基线/候选调用 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Luna/medium | 21,920,282 | 17,185,220 | 935,523 | 929,891 | 913 / 907 |
| Sol/high | 328,539 | 298,002 | 46,120 | 36,243 | 11 / 10 |

Luna非缓存输入下降约21.6%，输出下降约0.6%；没有增加强模型调用。
Luna输入合计基线25,909,274/候选23,234,500，其中缓存输入3,988,992/6,049,280；
Sol/high输入合计328,539/298,002，两组缓存输入均为0。不同模型分别报告，不以合并token掩盖档位变化。
这些数字证明成本方向可行，但**不是已上线节省**，也不代表语义质量通过。
9月8日下游预算0对15的重放产物一致，Phase 2不因研究预算改变保留/归包。

## Phase 2语义与留出集

冻结200条来源分层记录并排除开发病例及同包材料；随后重复身份审计发现17条论文/内容重合，
在候选评分前替换。保留frozen.json与frozen-v2.json及来源哈希，独立参考复用183条、另评17条。
来源-only Sol参考不看候选输出；52处标签/类型分歧已由本任务逐项回看原文，主要是细粒度体裁及缺媒体时
present/unclear边界。200条不是全体准确率估计；参考模型不是人工金标。
本任务的逐项复核仍是模型辅助复核，不冒充用户人工签核；原计划的独立人工验收尚未取得。
候选已因明确回退被否决，因此不以模型一致率代替该门槛，也不因此启用实验组件。

三天候选所有排除项均检查：9月6日7条，9月7日1条（祝贺），9月8日4条。多数是寒暄/玩笑，
但9月6日新排除的“Slack bookmarks很拥挤/有很多值得读的内容”仍可视为弱体验信号；
不把它包装成绝对无信息。原始记录均保留。候选总体增加保留不确定信息，也增加下游未研究库存。

| 日期 | 诊断对数 | 基线错误 | 候选错误 |
| --- | ---: | ---: | ---: |
| 9月6日 | 74 | 31 | 17 |
| 9月7日 | 71 | 12 | 36 |
| 9月8日 | 72 | 28 | 19 |

这是偏重分组变化的诊断样本，不可当人口准确率或跨日可比较百分比。9月7日的新增错误合并足以否决：

- `u_32507b12051271e65a44`明确引用《An Alien Mind》、讨论多模态CoT监控；
  `u_5d65ee2bfac36a7c5060`引用另一则自动研究实习生/2028路线消息，候选错误放进同一31条包。
- `u_788cae0b27c1fedb2b55`询问Windows/4090到AVP串流；
  `u_af98f68f943feb0869e3`是不同会话下的“Goes hard”，没有可见共同对象，却被并入。

原文已逐项查看。不能因其他日错误总数下降就放行这些回退；不再修改第三版候选。

## Phase 3验证

先使用40包回归＋20新包做正序/倒序20包压力测试。第一版精简合同暴露工具结果未展示后误判资料不足，
第二版补回工具结果读取与停止条件，未增设搜索或token上限。由于第一组20包已用于诊断，另冻结20包
最终留出，并排除相同身份/内容。第二版压力测试仍出现具体来源归属错误，结果不可视为通过。

再用**未改动的生产工作量估计器和加权器**冻结60包映射：11任务，大小为
8/1/8/8/7/8/6/5/6/2/1。基线与候选采用相同材料、同模型Sol/medium、相同映射及读者配置；
两组均60/60完成、无执行错误。未为了凑60包改变权重或扩大15任务预算。
两组各35份正式报告、25包核查后未发布。基线/候选证据台账各268/265条，其中自标
verified_fact为158/153条、source_claim为88/85条；这些是台账状态，不等于独立核实后的准确率。

| 生产加权正序 | 输入合计 | 缓存输入 | 非缓存输入 | 输出 |
| --- | ---: | ---: | ---: | ---: |
| 基线 | 33,151,785 | 31,058,432 | 2,093,353 | 189,098 |
| 候选 | 36,418,852 | 34,013,824 | 2,405,028 | 192,173 |

候选没有在该实验降本，用户也未要求Phase 3按token淘汰；选择看证据和深度，不看篇数或长度。
两组各11个thread均可追溯到本地会话，分别观察到1次compaction；未设置压缩次数淘汰条件。
来源工具返回按精确thread/cwd提取，抽取可能漏掉未被引用的镜像，逐项复核不能只信模型判分。
例如服务犬Charlie叙事被初次判为无来源，但完整会话确实有搜索镜像文本；它仍只是来源主张，
不是独立事实确认。此类提取缺失不计为模型凭空编造。

生产加权实验也出现关键事实回退：Armature（`p_38b825d71d6b68f09b0f`）的候选把
官方明确的synthetic repositories写成真实仓库，并误称逐session材料未公开；实际取得的官方
工具返回明确说明模拟用户及完整对话公开。基线保留了这两项限制。LibriBrain候选还遗漏
40/20/10分钟不同训练组与8名未微调受试者的边界。结合压力实验中的原帖来源错配，
**Phase 3精简合同同样NO-GO**；不再展开第三轮修订或用更多覆盖数字补偿。
两组60/60是执行覆盖，不是60包都已独立证据确证，更不是每日全量信息被深入研究。
60包盲评全部完成。原始模型评分的深度均值基线3.08/候选3.37、可读性3.68/3.92；
这些均值说明精简提示并非处处更差，却不能抵消Armature等关键回退，不作为通过依据。
最后一组5包合并输入超过CLI字符限制，保留已完成55包，改为逐包完成剩余5包，未重跑研究任务。
该调整仅属于离线评审分片，不改变Phase 3生产任务映射或模型。

真实中断恢复：同一thread `01a08191-e18f-74e3-b07b-dc8b72e30eb7`在15包验证后中断，
恢复后20/20完成，已验证的15包内容哈希均不变，观察到一次compaction。没有新增替代thread。
用量计数在resume时重置，已改为累计观测分段；约4,159,215输入、3,889,152缓存输入、30,466输出。
中断中的尚未上报计算可能缺失，故标为usage_complete=false，而不是假称完整精确计费。

## 验收证据与发布流程

所有实验位于runtime的`validation/convergence-20260908/`：phase2-audit.json、三日pairs-*/、
holdout-reference-v2/、weighted-paired-review/、各research variant、frozen-v2.json和interruption.json。
历史失败候选、第一次冻结、压力测试、修正前用量回执均保留。

安装版doctor、后台身份、只读引用沙箱、全链路无推送smoke及空队列切换须在实际部署时记录。
只有部署确认后才称本轮工程改进已上线；自然次日运行质量不能提前确认。

### 安装验证记录

候选安装快照为`app-cf3b5c067364-20260909T052116Z`，两个实验开关均false；
源码与安装模块字节比对一致。安装版doctor全部通过，四种模型/推理档位实际认证成功，
飞书用户身份与既定接收者匹配，知识空间可访问。临时LaunchAgent后台探针成功且已卸载；
`deployments/20260909-convergence/background_probe_receipt.json`记录零真实消息。

初次飞书检查失败不是已证实的账号失效：新目录可执行文件连`--version`也卡在macOS dyld，
旧快照相同SHA256字节则可正常启动。保留原文件`.pre-runtime-repair`，重新复制同哈希、签名有效
的1.0.68程序后，版本、认证、后台探针与doctor均恢复；未关闭签名或系统安全控制。
4个诊断子进程曾处于UE不可中断状态，已发送终止和SIGKILL，不为清理它们重启用户机器；
这不等于已确定dyld底层根因，需如实保留为本机环境限制。

默认关闭开关之外，另一次开启实验开关的隔离全链路smoke通过：7条输入、5包全部有结果，
3份报告、2包未发布，Wiki预览7页、无空页或未解析内部链接，真实飞书调用0。
它只证明路径可执行，不推翻语义NO-GO；正式切换依赖安装版默认配置的独立smoke。

### 最终切换（2026-09-09 13:55，Asia/Shanghai）

安装版默认配置smoke已通过：7条输入、6标准化单元、5包全部完成，3份报告、2包不发布；
brief核对通过，Wiki预览7页、空页0、未解析内部链接0、真实飞书调用0。
期间WebSocket连接重置导致首轮停滞，诊断中断后由现有队列恢复机制重试，两个调用均为
同一thread `01a0849f-a635-7d50-9d97-87941b5accb5`，没有额外research thread。
全链路耗时1,952.57秒（约32.5分钟），包含连接异常与恢复，不能外推为正常全量日报耗时。
最终队列路径为jobs→completed→archived，所有研究包已通过产物校验。

再次运行300项测试全部通过；ruff、mypy（43源文件）通过，34个顶层安装模块与当前源码
字节匹配。文档和离线评审脚本后续提交未改变生产模块。

已在活跃队列全部为空、三项旧服务空闲时完成`--cutover`。新快照
`app-cf3b5c067364-20260909T052116Z`被agent-runner/recover/tick实际加载；
三者校验时均not running、last exit 0，五类活跃队列仍全部为空。
`previous-v3`指向`app-a791e341c5cc-20260908T111433Z`，可按现有空队列回退流程恢复。
自动保留步骤删除0份快照；更早闲置08f快照移到upgrade-hold保留。原日程、Wiki结构、接收者不变。

本轮完成的是**失败候选不启用、工程保护单独上线**，不是证明Phase 2/3全局最优。
次日自然运行、真实推送与全量结果质量仍需实际运行确认，未提前记为通过。
机器可读发布记录：`deployments/20260909-convergence/release_receipt.json`。
